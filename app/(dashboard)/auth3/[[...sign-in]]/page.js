import React from 'react'

const SignInPage = ({params}) => {
    // if the page.js file in the auth3 folder is deleted then the /auth path is 404 to fix it 
    // use [[]], now /auth will open this page
  return (
    <h2 className='text-7xl'>SignIn Page</h2>
  )
}

export default SignInPage