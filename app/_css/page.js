import React from 'react'

//folder starting with the _ underscore will not be in the url routes just static folder with files

const page = () => {
  return (
    <div>page</div>
  )
}

export default page